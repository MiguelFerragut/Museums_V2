<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-8">
            <h1>Museums List</h1>
            <hr>
        </div>
    </div>
</div>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-8 justify-content-between">

            <table class="table">
                <tbody>
                    {{#each museums }}
                    <tr>
                        <td>
                            <h3>{{ name }}</h3>
                        </td>
                        <td>
                            <a href="/museums/details/{{_id}}" class="btn btn-dark btn-sm">Details</a>
                        </td>
                        <td>
                            <a href="/museums/edit/{{_id}}" class="btn btn-dark btn-sm">Edit</a>
                        </td>
                        <td>
                            <form action="/museums/delete/{{_id}}" method="POST">
                                <input type="submit" value="Delete" class="btn btn-danger btn-sm">
                            </form>
                        </td>
                    </tr>

                    {{/each}}
                </tbody>
            </table>

        </div>
    </div>
</div>